# Паспорт публикации — 12.09.2026

Приложение: «Гидросфера».

Владелец GitHub: `podsoblyaeva-sv`.

Репозиторий: https://github.com/podsoblyaeva-sv/hydrosphere

Адрес сайта: https://podsoblyaeva-sv.github.io/hydrosphere/

Хостинг: GitHub Pages, ветка `main`, корень репозитория `/`.

Тип: статический HTML/CSS/JavaScript. Сервер приложения, база данных, внешний API и секретные ключи не требуются. Домен и HTTPS обслуживаются GitHub Pages.

Зависимости: Three.js r128 включён локально; Google Fonts — внешняя необязательная загрузка шрифтов.

Источник: предоставленный владельцем артефакт Claude, идентификатор `8e6c5d31-1aaf-447a-90c0-beb7b306b1d3`. Исходные локальные файлы не изменялись. Публикуется отдельная подготовленная копия.

Изменения для самостоятельного запуска: оболочка HTML с UTF-8, `lang="ru"` и `viewport`; локальная Three.js; запуск анимации при первом открытии раздела 3D.

Проверено перед публикацией: синтаксис JavaScript; открытие страницы; 3D и выбор этапа испарения; прохождение всех 10 вопросов с одной намеренной ошибкой, результат 9/10; мобильный экран 390 пикселей без горизонтальной прокрутки; отсутствие ошибок JavaScript в проверенной сессии.

Обновление: изменять копию проекта, проверять в браузере, публиковать изменённые файлы в `main`. Для каждой новой публикации создавать новый датированный паспорт. Предыдущие паспорта сохранять.

Откат: выполнить `git revert` для коммита с изменениями и отправить результат в `main`; историю репозитория не переписывать.
